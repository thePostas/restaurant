import React from 'react';
import { MenuInner } from '../../containers/content/menuInner'

export const Menu = props => {
    return (
        <section className={'menu-block'}>
            <MenuInner title={'Our Menu'}/>
        </section>
    )
};